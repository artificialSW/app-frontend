import 'package:flutter/material.dart';

class PuzzleEditPage extends StatelessWidget {
  const PuzzleEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("퍼즐 편집 페이지입니다."),
    );
  }
}
